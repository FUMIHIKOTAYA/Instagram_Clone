<h1>投稿詳細画面</h1

<p>
  <strong>画像:</strong>
  <%= image_tag @picture.image_url %>
</p>
<p>内容: <%= @picture.content %></p>

<%= link_to %q(編集), edit_picture_path(@picutre) %>
<%= link_to %q(投稿一覧へ), picture_path %>
