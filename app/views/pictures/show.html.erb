<h1>投稿詳細画面</h1

<p>
  <strong>画像:</strong>
  <%= image_tag @picture.image_url %>
</p>
<p>内容: <%= @picture.content %></p>

<% unless @picture.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to %q(お気に入り解除する), favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to %q(お気に入りする), favorites_path(picture_id: @picture.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>

<%= link_to %q(投稿一覧へ), pictures_path %>
