<h1>投稿一覧</h1>

<table>
  <tr>
    <th>画像</th>
    <th>内容</th>
  </tr>
<% @pictures.each do |picture| %>
  <tr>
    <td><%= picture.image %></td>
    <td><%= picture.content %></td>
    <td><%= link_to %q(詳細), picture_path(picture.id) %></td>
    <td><%= link_to %q(編集), edit_picture_path(picutre.id) %></td>
  </tr>
<% end %>
</table>

<br>
<%= link_to %q(新規投稿), new_picture_path %>
