<h1>投稿一覧</h1>

<table>
  <tr>
    <th>画像</th>
    <th>内容</th>
  </tr>
  <% @pictures.each do |picture| %>
  <tr>
    <td><%= @picture.image.url %></td>
    <td><%= picture.content %></td>
  </tr>
  <% end %>
</table>
